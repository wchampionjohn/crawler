<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <%= render "shared/caption", site: '露天拍賣', title: '新增賣家帳號' %>

    <%= breadcrumb('露天拍賣', link_to("賣家管理", ruten_users_path), '新增帳號') %>
    <%= render 'form' %>
    </div>
  </div>

</div>
