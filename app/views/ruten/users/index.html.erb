<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <%= render "shared/caption", site: '露天拍賣', title: '賣家管理' %>

      <%= ruten_users_breadcrumb(params['user_id'])%>

    </div>
  </div>

  <div class="row">
    <div class="col-lg-12">
      <%= render partial: 'table', locals: { articles: current_collection } %>
    </div>
  </div>
</div>
